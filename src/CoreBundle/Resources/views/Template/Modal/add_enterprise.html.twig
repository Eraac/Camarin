{% extends 'CoreBundle:Template:Modal/layout.html.twig' %}

{% set modal_id = "addEnterprise" %}
{% set modal_title = "core.confirm.add_enterprise" %}

{%- block modal_content -%}
    {{ form_start(add_enterprise_form) }}
        {{ form_widget(add_enterprise_form) }}
        {% include 'CoreBundle:Template:form/button/new.html.twig' %}
    {{ form_end(add_enterprise_form) }}
{%- endblock -%}
